# =========================
# .gitignore для Python-проекта (2025)
# Описание: актуальные правила игнорирования для разработки ML/DS проектов.
# =========================

# --- Python: скомпилированные байт-код файлы и кеши
__pycache__/
*.py[cod]
*$py.class

# C-расширения
*.so
*.pyd

# --- Виртуальные окружения (локальные)
.venv/
venv/
env/
ENV/
/.venv/

# --- Jupyter / IPython
.ipynb_checkpoints/
.ipynb

# --- IDE / редакторы
.vscode/
.idea/
/.ropeproject/

# --- Пакеты/сборки/дистрибутивы
build/
dist/
*.egg-info/
*.egg
sdist/
wheelhouse/
pip-wheel-metadata/

# --- Тесты, покрытие и кэш тестов
.pytest_cache/
.mypy_cache/
.cache/
.coverage
htmlcov/

# --- Логи, БД и временные файлы
logs/
*.log
*.sqlite3
*.db

# --- Модели, артефакты и результаты (обычно большие, не в репо)
models/           # Сохранённые .pkl модели (LinearRegression, GradientBoosting, XGBoost, LightGBM, Ensemble)
results/          # Результаты: CSV, PNG графики, SHAP plots, отчёты
backups/          # Резервные копии
artifacts/        # Прочие артефакты
presentation/     # Презентации (если большие файлы)

# --- Конфигурации окружения и секреты
.env
*.env
.env.*
.secrets
secrets/
credentials.json

# --- ML / экспериментальные фреймворки
mlruns/        # mlflow
.dvc/           # dvc метаданные

# --- DVC cache (если используется)
.git/data/      # пример пути, адаптируйте при необходимости

# --- Системные файлы
.DS_Store
Thumbs.db
desktop.ini

# --- Докер и контейнеры
docker-compose.override.yml
Dockerfile.dev
.docker/

# --- Кеши пакетных менеджеров/сборщиков
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Пайплайн / CI временные файлы
.terraform/
.nox/

# --- Иные бинарные/временные артефакты
*.pkl           # Joblib/pickle модели (LightGBM, XGBoost и т.д.)
*.joblib        # Joblib сохранения
*.h5            # HDF5 модели (Keras/TensorFlow)
*.model         # Иные форматы моделей
*.csv           # CSV результаты (в results/)
*.png           # PNG графики (в results/plots/)

# --- OS-specific
ehthumbs.db
Icon?